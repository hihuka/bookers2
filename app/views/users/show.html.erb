<div class="container">  <!-- この行を追加 -->
  <div class="row">      <!-- この行を追加 -->
  <div class="col-md-4">
    <%= render 'user', user: @user %>
    <%= render 'books/book', book: @book %>
  </div>
<div class="col-md-8">
  <h2>Books</h2>
<div class="container">
  <div class="row">
   <table class="table table-striped">
     <thead>
        <tr>
          <th colspan="3"></th>
           </td>
          <th>Title</th>
          <th>opinion</th>
        </tr>
      </thead>
      <tbody>
        <% @users.each do |user| %>
        <tr>
          <td>
            <%= link_to user_path(@user.id) do %>
            <%= image_tag @user.get_profile_image(100,100) %>
            <% end %>
          </td>
          <td>
            <%= link_to @users.books.title,book_path(book.id) %>
          </td>
          <td><%= book.opinion %></td>
        </tr>
        <% end %>
      </tbody>
   </table>
  </div>
</div>
</div>